% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.get_bash_script}
\alias{.get_bash_script}
\title{Generate Bash Script for Model Fitting}
\usage{
.get_bash_script(
  cluster_run,
  job_cores = NULL,
  job_email = NULL,
  job_name = NULL,
  K_max,
  btblv_data_path,
  iter,
  warmup,
  thin,
  chains,
  precision,
  seed,
  mc_samples,
  config_path,
  model_name_pattern,
  save_gdrive,
  gdrive_folder_id,
  local_path,
  max_treedepth = 10
)
}
\arguments{
\item{cluster_run}{Logical. If \code{TRUE}, the bash script is configured for cluster execution using a job
scheduler (e.g., SLURM). If \code{FALSE}, the script is configured for local execution.}

\item{job_cores}{Integer. The number of CPU cores to request for the job (required if \code{cluster_run = TRUE}).}

\item{job_email}{Character. The email address to receive job notifications (required if \code{cluster_run = TRUE}).}

\item{job_name}{Character. The name of the job (required if \code{cluster_run = TRUE}).}

\item{K_max}{Integer. The maximum number of clusters to fit models for.}

\item{btblv_data_path}{Character. The path to the data file for model fitting.}

\item{iter}{Integer. The number of iterations for the MCMC sampling.}

\item{warmup}{Integer. The number of warmup iterations for the MCMC sampling.}

\item{thin}{Integer. The thinning interval for the MCMC sampling.}

\item{chains}{Integer. The number of chains for the MCMC sampling.}

\item{precision}{Character. The precision parametrization for the model. It can be \code{"specific"} or \code{"single"}.}

\item{seed}{Integer. The seed for reproducibility.}

\item{mc_samples}{Integer. The number of Monte Carlo samples for model diagnostics.}

\item{config_path}{Character. The path to the configuration file.}

\item{model_name_pattern}{Character. The pattern for naming the saved model files.}

\item{save_gdrive}{Logical. If \code{TRUE}, the models are saved to Google Drive. If \code{FALSE}, they are saved locally.}

\item{gdrive_folder_id}{Character. The Google Drive folder ID for saving models (required if \code{save_gdrive = TRUE}).}

\item{local_path}{Character. The local path for saving models (required if \code{save_gdrive = FALSE}).}

\item{max_treedepth}{Integer. The maximum tree depth for the MCMC sampling (default: 10).}
}
\value{
A character string containing the generated bash script.
}
\description{
This function generates a bash script to fit and save Bayesian models either locally or on a cluster.
The script can be customized for local execution or for submission to a job scheduler (e.g., SLURM).
}
\examples{
\dontrun{
# Example 1: Generate a bash script for local execution
bash_script = .get_bash_script(
  cluster_run = FALSE,
  K_max = 5,
  btblv_data_path = "/path/to/data.csv",
  iter = 2000,
  warmup = 1000,
  thin = 1,
  chains = 4,
  precision = "double",
  seed = 123,
  mc_samples = 1000,
  config_path = "/path/to/config.yaml",
  model_name_pattern = "model_K",
  save_gdrive = FALSE,
  local_path = "/path/to/save/models"
)
cat(bash_script)

# Example 2: Generate a bash script for cluster execution
bash_script = .get_bash_script(
  cluster_run = TRUE,
  job_cores = 4,
  job_email = "user@example.com",
  job_name = "btblv_fit",
  K_max = 5,
  btblv_data_path = "/path/to/data.csv",
  iter = 2000,
  warmup = 1000,
  thin = 1,
  chains = 4,
  precision = "double",
  seed = 123,
  mc_samples = 1000,
  config_path = "/path/to/config.yaml",
  model_name_pattern = "model_K",
  save_gdrive = TRUE,
  gdrive_folder_id = "your-google-drive-folder-id"
)
cat(bash_script)
}

}
